大模型下的点餐流程
1. 出示菜单----使用图片链接的方式
2. 用户选择菜品
    2.1 获取所有菜品----加入额外的url，用于获取菜品
    2.2 大模型判断是否选择了这个菜品
    2.3 加入购物车----引入流程中的中间变量，触发中间变量新增的动作
3. 重复2的过程，直到用户说可以了----大模型判断
4. 展示购物车列表
5. 用户取消菜品----触发中间变量删除动作
6. 重复4-5的过程直到用户说确认、可以了等等
7. 发起支付
8. 确认支付，成功下单