目前可能要自己做一个简易版本的，大概思路如下，帮我看看是否合理，或者有没有进一步优化的空间：

1. 使用 fastapi -> service ->tortoise-orm-> postgresql 写入模型源

2. 写入时自动触发获取模型列表，也可通过接口手动更新

3. 模型默认全部关闭，通过点击启动，启动时，测延迟

4. 模型有别名

5. 定时进行可用模型列表的延迟测试，并写入到 postgresql 

6. 项目使用模型时，通过别名+正式名检索，找到延迟最小的模型源，然后连接使用