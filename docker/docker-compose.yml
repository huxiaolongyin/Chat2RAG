name: chat2rag
services:
  service:
    image: jacob0827/chat2rag:latest
    container_name: service
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - ${DATA_PATH}/phoenix:/.phoenix
      - ${DATA_PATH}/checkpoint:/code/data/checkpoint
      - ${LOG_PATH}:/code/logs
      - ${MIGRATIONS_PATH}:/code/migrations
      - ${UPLOADS_PATH}:/code/uploads
      - ${MODEL_PATH}:/code/models
    depends_on:
      qdrant:
        condition: service_healthy
    environment:
      - TZ=Asia/Shanghai
      - QDRANT_LOCATION=http://qdrant:6333
      - POSTGRESQL_HOST=postgresql
    env_file:
      - ../.env
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8000/api/v1/health" ]
      interval: 30s
      timeout: 3s
      retries: 5
      start_period: 30s
    networks:
      - chat2rag-network
    # restart: always

  qdrant:
    image: qdrant/qdrant:v1.16
    container_name: qdrant
    healthcheck:
      test: [ "CMD", "/bin/bash", "-c", '</dev/tcp/localhost/6333' ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - ${DATA_PATH}/qdrant_storage:/qdrant/storage
    env_file:
      - ../.env
    networks:
      - chat2rag-network
    restart: always

  postgresql:
    image: timescale/timescaledb:latest-pg17-bitnami
    container_name: postgresql
    ports:
      - "5432:5432"
    volumes:
      - ${DATA_PATH}/postgresql:/bitnami/postgresql
    environment:
      - POSTGRESQL_PASSWORD=${POSTGRESQL_PASSWORD}
      - POSTGRESQL_DATABASE=${POSTGRESQL_DATABASE}
      - POSTGRESQL_SHARED_BUFFERS=1GB # 设置数据库共享内存缓冲区大小，建议设置为服务器内存的25%，更大的值可以提高读取性能
      - POSTGRESQL_MAX_CONNECTIONS=50 # 控制数据库最大并发连接数，需要根据服务器资源和应用需求设置
      - TZ=Asia/Shanghai # 设置容器时区为中国时区
      - POSTGRESQL_TIMEZONE=Asia/Shanghai # 设置PostgreSQL时区
      - POSTGRESQL_INITDB_ARGS=--data-checksums --encoding=UTF8 --locale=C --timezone=Asia/Shanghai # 初始化参数
    env_file:
      - ../.env
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres && PGPASSWORD=${POSTGRESQL_PASSWORD} psql -U postgres -d ${POSTGRESQL_DATABASE} -c 'SELECT 1'" ]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - chat2rag-network
    restart: always
    
networks:
  chat2rag-network:
    driver: bridge
